import 'package:flutter/material.dart';

import 'colors.dart';

final kDecoration = BoxDecoration(color: kCardBackgrounfColor);
final List<BoxShadow> kBoxshadow = [
  BoxShadow(
    color: Colors.grey.shade600,
    blurRadius: 2,
    spreadRadius: 2.0,
    offset: const Offset(
      3,
      3,
    ),
  )
];

